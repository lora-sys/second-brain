# Screenshots

The Knowledge Graph view in the cockpit is exercised by the e2e
suite. Live-browser screenshots are blocked in this dev session by
the HTTP-proxy sandbox (same caveat as v0.15 / v0.17 / v0.30).
Run:

```sh
playwright-cli run-code --filename tests/e2e/real-device.mjs
```

# Dependencies - run npm install after cloning
node_modules/

# Runtime data
data/
logs/

# Recordings (kept in docs/assets/ for repo; private recordings stay out)
recordings/*.webm
!recordings/.gitkeep
!docs/assets/demo.webm

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Local config overrides - each user sets their own vaultPath via Settings
config.json

# Local test content (not part of the project)
content/
.playwright-cli/
